// export const routes = 'home contact view code about error'
//   .split(' ')
//   .reduce((all, key) => {
//     all[key] = '/' + key;
//     return all;
//   }, {});

export const routes = {
  home: '/home',
  contact: '/contact',
  view: '/view',
  code: '/code',
  about: '/about',
  error: '/error',
};
